# LogseqBrain v0.7.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship LogseqBrain v0.7.0 — durable config location, Logseq normalization tolerance, `HH:mm` journey-log prefix, and a brain-stats analytics mode on `brain-status`.

**Architecture:** Pure-markdown Claude Code plugin — no build, no runtime code, no test runner. Each task edits `SKILL.md` / `skills/_shared/*.md` / docs. "Validation" means invoking the affected skill against a scratch ClaudeBrain graph per `CONTRIBUTING.md`, then committing.

**Tech Stack:** Markdown skills, `.claude-plugin/plugin.json`, Logseq graph (markdown files). Validation harness = a scratch graph at `LOGSEQ_BRAIN_PATH`.

**Spec:** `docs/superpowers/specs/2026-05-31-v0.7.0-design.md`

---

## File Structure

- **Modify** `skills/_shared/path-resolution.md` — durable config resolution order, new config-file location, one-time legacy migration.
- **Modify** `skills/_shared/journey-log.md` — read config from new location; re-add `HH:mm` prefix; pointer to logseq-format.md.
- **Create** `skills/_shared/logseq-format.md` — Logseq normalization behaviors + survival rules.
- **Modify** `skills/brain-save/SKILL.md` — pointer to logseq-format.md.
- **Modify** `skills/brain-status/SKILL.md` — analytics-mode triggers + branch.
- **Modify** `.claude-plugin/plugin.json` — version bump.
- **Modify** `README.md`, `CONTRIBUTING.md`, `CLAUDE.md`, `ROADMAP.md`, `docs/superpowers/specs/2026-05-01-v0.6.0-design.md` — doc alignment.

> **Note on activity-line examples:** the per-skill `SKILL.md` files (`brain-init`, `brain-load`, `brain-save`, `brain-status`) pass *content strings* (e.g. `viewed dashboard`) to journey-log; the bullet marker and `HH:mm` prefix are added inside `journey-log.md`. So the HH:mm change lives only in `journey-log.md` — the per-skill files need NO format change for Section 3. The spec's "examples in all four SKILL.md" is satisfied by confirming none of them hardcode a bullet/prefix.

---

## Task 1: Durable config location (path-resolution.md)

**Files:**
- Modify: `skills/_shared/path-resolution.md`

- [ ] **Step 1: Replace the "In Claude Code / GitHub Copilot CLI / Gemini CLI" resolution order**

Replace this block:

```markdown
### In Claude Code / GitHub Copilot CLI / Gemini CLI

Try in this order; stop at the first success:

1. **Argument:** the user provided a path inline (e.g., "load brain at /path/to/graph"). Use it directly.
2. **Environment variable:** read `LOGSEQ_BRAIN_PATH`. If set and the path exists, use it.
3. **Config file:** read `.brain-config.json` in the plugin root. If it contains `{"graphPath": "..."}` and the path exists, use it.
4. **Ask the user.** Prompt: "Where is your ClaudeBrain Logseq graph folder?"

Once resolved, all other brain operations in this session use that path.
```

with:

```markdown
### In Claude Code / GitHub Copilot CLI / Gemini CLI

Try in this order; stop at the first success:

1. **Argument:** the user provided a path inline (e.g., "load brain at /path/to/graph"). Use it directly.
2. **Environment variable:** read `LOGSEQ_BRAIN_PATH`. If set and non-empty and the path exists, use it. Do NOT read or write any config file when the env var wins.
3. **User config file:** read `graphPath` from the durable user config file (see "Config file location" below). If present and the path exists, use it.
4. **One-time legacy migration:** if no user config file exists yet, check for a legacy `.brain-config.json` at the plugin root. If found and valid, copy its values into the user config file (see "Config file location"), then use its `graphPath`. Silent best-effort — on any failure, fall through to step 5.
5. **Ask the user.** Prompt: "Where is your ClaudeBrain Logseq graph folder?" After they answer and the path is confirmed to exist, **persist** it to the user config file (creating the directory if needed).

Once resolved, all other brain operations in this session use that path.
```

- [ ] **Step 2: Replace the "Config file shape" section with a "Config file location" + "Config file shape" pair**

Replace this block:

```markdown
## Config file shape

`.brain-config.json` (plugin-root, gitignored) supports these keys:

```json
{
  "graphPath": "/absolute/path/to/ClaudeBrain",
  "journeyLog": true
}
```

- `graphPath` (string, required for non-Cowork hosts): absolute path to the graph folder.
- `journeyLog` (boolean, optional, default `true`): whether to write activity-trail entries on each brain skill use. See `skills/_shared/journey-log.md`.
```

with:

```markdown
## Config file location

The durable user config file lives **outside the plugin cache** so it survives `/reload-plugins` and version bumps:

- **Windows:** `%APPDATA%\logseq-brain\config.json`
- **macOS / Linux:** `$XDG_CONFIG_HOME/logseq-brain/config.json` if `XDG_CONFIG_HOME` is set, otherwise `~/.config/logseq-brain/config.json`

Create the `logseq-brain` directory if it does not exist when persisting. Read with the Read tool; write with the Write tool.

> **Legacy:** older versions stored `.brain-config.json` at the plugin cache root. That file is wiped on reload, so it is now only a one-time migration source (resolution step 4), never the source of truth.

## Config file shape

```json
{
  "graphPath": "/absolute/path/to/ClaudeBrain",
  "journeyLog": true
}
```

- `graphPath` (string, required for non-Cowork hosts): absolute path to the graph folder.
- `journeyLog` (boolean, optional, default `true`): whether to write activity-trail entries on each brain skill use. See `skills/_shared/journey-log.md`.
```

- [ ] **Step 3: Update the "Conflicting config + env" failure mode**

Replace:

```markdown
- **Conflicting config + env:** env var wins (per the resolution order above).
```

with:

```markdown
- **Conflicting config + env:** the `LOGSEQ_BRAIN_PATH` env var wins and the config file is not consulted (per the resolution order above).
- **Config directory not writable:** if persisting to the user config file fails, tell the user the path was resolved for this session but could not be saved, and suggest setting `LOGSEQ_BRAIN_PATH`. Don't block the operation.
```

- [ ] **Step 4: Validate**

Manual round-trip (per CONTRIBUTING):
1. Unset `LOGSEQ_BRAIN_PATH` and ensure `%APPDATA%\logseq-brain\config.json` does not exist. Run `brain-load` / "load brain" — confirm Claude prompts for the path, then writes `%APPDATA%\logseq-brain\config.json` with `graphPath`.
2. Run a second brain operation in a fresh session — confirm it reads the path from the config file without prompting.
3. Set `$env:LOGSEQ_BRAIN_PATH` to a different valid graph — confirm it wins and the config file is not read.
Expected: all three behave as described; no reference to `.brain-config.json` as a live source remains.

- [ ] **Step 5: Commit**

```bash
git add skills/_shared/path-resolution.md
git commit -m "feat(config): durable user-config-dir path resolution with env override and legacy migration"
```

---

## Task 2: journey-log config read + HH:mm prefix

**Files:**
- Modify: `skills/_shared/journey-log.md`

- [ ] **Step 1: Update the config read (Algorithm step 1) to the new location**

Replace:

```markdown
1. **Read config.** Read `.brain-config.json` in the plugin root if present. If it contains `"journeyLog": false`, return immediately — no write.
```

with:

```markdown
1. **Read config.** Read the durable user config file (location per `skills/_shared/path-resolution.md` → "Config file location") if present. If it contains `"journeyLog": false`, return immediately — no write.
```

- [ ] **Step 2: Add the `HH:mm` prefix to the journal-creation template (Algorithm step 3)**

Replace:

```markdown
   ```markdown
   - ## Sessions
   - ## Activity
     - {{activity-line}}
   ```
```

with:

```markdown
   ```markdown
   - ## Sessions
   - ## Activity
     - HH:mm {{activity-line}}
   ```

   where `HH:mm` is the current local time in 24-hour format (e.g. `14:32`).
```

- [ ] **Step 3: Update the append-anchoring prose (Algorithm steps 4 & 5) to include the prefix**

Replace:

```markdown
4. **If the journal file exists but lacks `## Activity`:** insert a new top-level bullet `- ## Activity` after the **entire** `## Sessions` block — i.e., immediately before the next top-level `- ##` heading, or at end of file if `## Sessions` is the last block (or missing entirely). The insertion must be at indent 0 (no leading spaces), regardless of how deeply `## Sessions` is nested at the time. Then append the new bullet as a child at indent 2.

5. **If the journal file exists with `## Activity`:** append a child bullet at the end of the `## Activity` section, before the next top-level `- ##` heading or end of file.

   Surgical Edit anchoring: the `old_string` must be unique within the file, so don't use just the last bullet's text (it may repeat — e.g., two `viewed dashboard` calls in a day). Read the last 3 lines of the `## Activity` section and use the multi-line block as `old_string`; replace with the same block plus the new bullet appended (indent 2). If the section is empty, the unique anchor is `- ## Activity\n` followed by the next top-level `- ##` heading or end of file — replace `- ## Activity\n` with `- ## Activity\n  - <activity-line>\n`.
```

with:

```markdown
4. **If the journal file exists but lacks `## Activity`:** insert a new top-level bullet `- ## Activity` after the **entire** `## Sessions` block — i.e., immediately before the next top-level `- ##` heading, or at end of file if `## Sessions` is the last block (or missing entirely). The insertion must be at indent 0 (no leading spaces), regardless of how deeply `## Sessions` is nested at the time. Then append the new bullet as a child at indent 2: `  - HH:mm <activity-line>`.

5. **If the journal file exists with `## Activity`:** append a child bullet at the end of the `## Activity` section, before the next top-level `- ##` heading or end of file.

   Surgical Edit anchoring: the `old_string` must be unique within the file, so don't use just the last bullet's text (it may repeat — e.g., two `viewed dashboard` calls in a day; the `HH:mm` prefix usually differs but don't rely on it). Read the last 3 lines of the `## Activity` section and use the multi-line block as `old_string`; replace with the same block plus the new bullet appended (indent 2, with `HH:mm ` prefix). If the section is empty, the unique anchor is `- ## Activity\n` followed by the next top-level `- ##` heading or end of file — replace `- ## Activity\n` with `- ## Activity\n  - HH:mm <activity-line>\n`.

   Before editing, account for Logseq normalization — see `skills/_shared/logseq-format.md`. Read the region immediately before the Edit and anchor on the `## Activity` heading text, not on a `- ` prefix or indentation.
```

- [ ] **Step 4: Replace the "no time prefix" format rule**

Replace:

```markdown
- No leading time prefix — Logseq auto-assigns `created-at::` block metadata when the file is parsed.
```

with:

```markdown
- Each bullet begins with an `HH:mm ` local-time prefix (24-hour, e.g. `14:32 `). This restores the quick-scan chronological signal; v0.6.0 dropped it on the assumption Logseq would render `created-at::` inline, which it does not. See `docs/superpowers/specs/2026-05-31-v0.7.0-design.md` §3.
```

- [ ] **Step 5: Update the config-failure mode wording**

Replace:

```markdown
- **`.brain-config.json` invalid JSON:** treat as if `journeyLog` is `true` (default) and log the error to the user, but don't block the main skill.
```

with:

```markdown
- **User config file invalid JSON:** treat as if `journeyLog` is `true` (default) and log the error to the user, but don't block the main skill.
```

- [ ] **Step 6: Validate**

Manual round-trip:
1. Delete today's journal in the scratch graph. Run `brain-status` ("brain status"). Confirm `journals/yyyy_MM_dd.md` is created with `  - HH:mm viewed dashboard` (real current time).
2. Run `brain-status` again. Confirm a second `  - HH:mm viewed dashboard` bullet appends under the same `## Activity` with a (likely) different time, and `## Sessions` is untouched.
3. Set `"journeyLog": false` in the user config file. Run again. Confirm NO new bullet.
Expected: prefixed bullets in 1 & 2; no bullet in 3.

- [ ] **Step 7: Commit**

```bash
git add skills/_shared/journey-log.md
git commit -m "feat(journey-log): re-add HH:mm time prefix; read durable config; normalization caveat"
```

---

## Task 3: New skills/_shared/logseq-format.md + pointers

**Files:**
- Create: `skills/_shared/logseq-format.md`
- Modify: `skills/brain-save/SKILL.md`
- (journey-log.md pointer already added in Task 2, Step 3)

- [ ] **Step 1: Create `skills/_shared/logseq-format.md`**

```markdown
# Logseq Format & Normalization

When Logseq parses pages and journals that a brain skill wrote, it **normalizes** the markdown. Skills must edit against the normalized form, not against what they originally wrote. This reference documents the normalizations and the rules that survive them.

## What Logseq normalizes on parse

- **Heading bullets lose the `- ` marker.** A line we wrote as `- ## Activity` is re-serialized by Logseq as `## Activity` (no leading bullet) once the file is opened and synced.
- **Leading-space indents become tabs.** Two-space child indents (`  - foo`) are converted to a single tab (`\t- foo`).
- **Empty section headings are stripped.** An empty `## Sessions` heading with no children may be removed entirely.
- **Trailing whitespace and blank-line runs are collapsed.**

## Survival rules (always follow before an Edit)

1. **Read immediately before Edit.** Never build an `old_string` from remembered or previously-written content. Read the target region in the same turn as the Edit so it reflects Logseq's current normalized form.
2. **Anchor on heading text, not the bullet prefix.** Match on `## Activity` / `## Session Log` / `## Current Plan` — never assume a leading `- `. The same heading may appear as `- ## Activity` (freshly written by us) or `## Activity` (after Logseq round-trip); your anchor must tolerate both, so anchor on the `## <Name>` substring.
3. **Don't assume the indent character.** When matching child bullets, prefer the heading + first/last child you actually read rather than hardcoding two spaces vs. a tab.
4. **A missing expected heading means Logseq removed it, not corruption.** If you expected an empty `## Sessions` and it's gone, recreate it as part of the Edit rather than aborting.

## Why this matters

The plugin's whole value is cross-device continuity via Logseq Sync. Files we write get opened, normalized, and synced by Logseq on other devices before we touch them again. Surgical Read-before-Edit (rules above) is what keeps our writes idempotent across that round-trip.
```

- [ ] **Step 2: Add a pointer in brain-save/SKILL.md**

Read `skills/brain-save/SKILL.md` and locate the section that describes surgical Edits / anchoring (the save algorithm). Add this line where the algorithm first relies on matching an existing section heading:

```markdown
> Before any Edit, account for Logseq's parse-time normalization (dropped `- ` on headings, space→tab indents, stripped empty headings). Read the target region immediately before editing and anchor on heading text — see `skills/_shared/logseq-format.md`.
```

(If a near-identical caveat already exists, leave it and just append the `see skills/_shared/logseq-format.md` reference rather than duplicating.)

- [ ] **Step 3: Validate**

Manual round-trip:
1. In the scratch graph, open a project page in the Logseq desktop app so Logseq normalizes it (headings lose `- `, indents become tabs). Close/sync.
2. Run `brain-save` ("save this") against that project. Confirm the new Session Log entry appends correctly with no duplicated headings and no whole-page rewrite (check the Edit was section-anchored).
Expected: clean surgical append against the normalized file.

- [ ] **Step 4: Commit**

```bash
git add skills/_shared/logseq-format.md skills/brain-save/SKILL.md
git commit -m "feat(format): add _shared/logseq-format.md normalization reference; link from brain-save"
```

---

## Task 4: brain-status analytics mode

**Files:**
- Modify: `skills/brain-status/SKILL.md`

- [ ] **Step 1: Add analytics triggers to the frontmatter description**

Replace:

```markdown
description: >
  Show a dashboard of all projects in the Claude Brain graph. Triggers: "brain
  status", "show projects", "show brain", "what's in my brain", "project
  dashboard", "brain overview", "list projects", "summary". Don't fire for loads
  (use brain-load) or saves (use brain-save).
```

with:

```markdown
description: >
  Show a dashboard of all projects in the Claude Brain graph, or graph
  analytics. Dashboard triggers: "brain status", "show projects", "show brain",
  "what's in my brain", "project dashboard", "brain overview", "list projects",
  "summary". Analytics triggers: "brain stats", "graph analytics", "activity
  over time". Don't fire for loads (use brain-load) or saves (use brain-save).
```

- [ ] **Step 2: Add a "Mode selection" note above "Dashboard Generation"**

Insert after the `# Brain Status` intro paragraph and before `## Prerequisites`:

```markdown
## Modes

- **Dashboard** (default, "brain status" / "show projects" / …): the per-project overview in "Dashboard Generation" below.
- **Analytics** ("brain stats" / "graph analytics" / "activity over time"): the aggregate counts in "Analytics (brain stats)" below.

Both resolve the graph path first (Prerequisites). Pick the mode from the trigger phrase; if ambiguous, default to Dashboard.
```

- [ ] **Step 3: Add the "Analytics (brain stats)" section after "Example Output"**

Insert before `## Important Notes`:

```markdown
## Analytics (brain stats)

Read-only aggregate view. Writes nothing except the journey-log entry. Stay token-frugal — use `skills/_shared/section-locator.md` for targeted reads; never full-read project pages.

1. **Projects.** Glob `pages/Projects___*.md`. Count total. Apply `skills/_shared/staleness.md` to split active vs. stale.
2. **Decisions.** Read `pages/Decisions.md`. Count total decision entries and break down by `status::` value (e.g. accepted, superseded). Also count per-project decisions inside project pages' `## Decisions` sections.
3. **Sessions.** For each project page, count entries under `## Session Log` (section-targeted read). Sum across projects.
4. **Activity (recent window).** Glob `journals/*.md`. For journals dated within the last 30 days (filename `yyyy_MM_dd.md`), count bullets under `## Activity`. Report the total as the recent activity signal.
5. **Present** a compact block:

   ```
   Brain stats:

   Projects: <N> (<active> active, <stale> stale)
   Decisions: <D> total (<accepted> accepted, <superseded> superseded, …)
   Sessions logged: <S>
   Activity (last 30 days): <A> entries
   ```

6. **Write a journey-log entry** per `skills/_shared/journey-log.md` with activity line: `viewed brain stats`.
```

- [ ] **Step 4: Validate**

Manual round-trip:
1. In a scratch graph with ≥2 projects, some decisions, and a few journal activity bullets, run "brain stats".
   Expected: the compact stats block with correct counts (spot-check against the files); active/stale split matches staleness rules; a `HH:mm viewed brain stats` bullet lands in today's journal.
2. Run "brain status" (plain). Expected: the normal dashboard, NOT the stats block — confirms the two modes route correctly.

- [ ] **Step 5: Commit**

```bash
git add skills/brain-status/SKILL.md
git commit -m "feat(brain-status): add brain-stats analytics mode (counts + 30-day activity)"
```

---

## Task 5: Docs alignment + version bump

**Files:**
- Modify: `.claude-plugin/plugin.json`
- Modify: `README.md`
- Modify: `CONTRIBUTING.md`
- Modify: `CLAUDE.md`
- Modify: `ROADMAP.md`
- Modify: `docs/superpowers/specs/2026-05-01-v0.6.0-design.md`

- [ ] **Step 1: Bump version in plugin.json**

In `.claude-plugin/plugin.json`, replace:

```json
  "version": "0.6.0",
```

with:

```json
  "version": "0.7.0",
```

- [ ] **Step 2: Update README setup instructions**

Replace:

```markdown
- **Environment variable**: `export LOGSEQ_BRAIN_PATH=/path/to/ClaudeBrain`
- **Config file**: create `.brain-config.json` in the plugin root with `{"graphPath": "/path/to/ClaudeBrain"}`
- **Just tell Claude the path** when prompted
```

with:

```markdown
- **Environment variable**: `export LOGSEQ_BRAIN_PATH=/path/to/ClaudeBrain` (highest precedence)
- **Just tell Claude the path** when prompted — Claude saves it to a durable user config file (`%APPDATA%\logseq-brain\config.json` on Windows, `~/.config/logseq-brain/config.json` on macOS/Linux) so you're not asked again, even after plugin reloads
```

- [ ] **Step 3: Update README Journey Log toggle note**

Replace:

```markdown
Every brain operation (init / load / save / status / search) leaves a one-line bullet in today's journal under `## Activity` — a low-cost audit trail of what Claude did, when. Disable by adding `"journeyLog": false` to `.brain-config.json`.
```

with:

```markdown
Every brain operation (init / load / save / status / search) leaves a one-line `HH:mm`-prefixed bullet in today's journal under `## Activity` — a low-cost, time-ordered audit trail of what Claude did, when. Disable by adding `"journeyLog": false` to your user config file (`%APPDATA%\logseq-brain\config.json` / `~/.config/logseq-brain/config.json`).
```

- [ ] **Step 4: Update CONTRIBUTING config-toggle checklist item**

Replace:

```markdown
10. **Config toggle.** Set `.brain-config.json` `{"journeyLog": false}`. Re-run any of the above. Verify `## Activity` does NOT gain a new bullet. Restore to `journeyLog: true` and verify activity logging resumes.
```

with:

```markdown
10. **Config toggle.** Set `"journeyLog": false` in the user config file (`%APPDATA%\logseq-brain\config.json` / `~/.config/logseq-brain/config.json`). Re-run any of the above. Verify `## Activity` does NOT gain a new bullet. Restore to `journeyLog: true` and verify activity logging resumes.
11. **Durable config.** Resolve a path by answering the prompt; confirm it persists to the user config file. Simulate `/reload-plugins` (or delete the plugin cache) and re-run — confirm no re-prompt. Set `LOGSEQ_BRAIN_PATH` to a different graph and confirm it overrides the file.
12. **Brain stats.** Run "brain stats" against a graph with ≥2 projects; confirm counts match the files and "brain status" still shows the plain dashboard.
```

- [ ] **Step 5: Update CLAUDE.md path-resolution description**

In `CLAUDE.md`, replace:

```markdown
See `skills/_shared/path-resolution.md` — branches by host (Cowork uses `request_cowork_directory`; Claude Code / Copilot CLI / Gemini CLI use env var → config file → ask). Once resolved, all other brain operations in the session use that path. When editing skills, preserve this host-aware branching — don't collapse it into a single chain.
```

with:

```markdown
See `skills/_shared/path-resolution.md` — branches by host (Cowork uses `request_cowork_directory`; Claude Code / Copilot CLI / Gemini CLI use `LOGSEQ_BRAIN_PATH` env var → durable user config file at `%APPDATA%\logseq-brain\config.json` / `~/.config/logseq-brain/config.json` → ask-and-persist). The config file lives outside the plugin cache so it survives `/reload-plugins`. Once resolved, all other brain operations in the session use that path. When editing skills, preserve this host-aware branching — don't collapse it into a single chain.
```

- [ ] **Step 6: Add the `_shared/logseq-format.md` line to CLAUDE.md shared-references list**

In `CLAUDE.md`, in the "Current shared references" list, after the `section-locator.md` bullet add:

```markdown
- `skills/_shared/logseq-format.md` — Logseq parse-time normalization behaviors + read-before-edit survival rules (used by `brain-save`, `journey-log`)
```

- [ ] **Step 7: Move v0.7.0 to Shipped in ROADMAP.md**

In `ROADMAP.md`, add a new `### v0.7.0` block at the top of the `## Shipped` section:

```markdown
### v0.7.0 — Durability, format tolerance, first analytics
- Durable config: `LOGSEQ_BRAIN_PATH` → user config dir (`%APPDATA%\logseq-brain\config.json` / `~/.config/logseq-brain/config.json`) → ask-and-persist; survives `/reload-plugins`; one-time legacy `.brain-config.json` migration
- `skills/_shared/logseq-format.md` — normalization-tolerance reference (read-before-edit, anchor on heading text)
- `HH:mm` time prefix restored on journey-log activity bullets (reverses v0.6.0 call)
- `brain-stats` analytics mode on `brain-status` (project/decision/session counts + 30-day activity window)
```

Then replace the `## Current — v0.7.0 (backlog)` section heading and its body with a fresh `## Current — v0.8.0 (TBD)` placeholder that keeps only the still-open candidates:

```markdown
## Current — v0.8.0 (TBD)

Open candidates from the Future list will be promoted here once Logseq's roadmap clarifies which is closest to ready.
```

(Remove the "Graph analytics" bullet from the Future list since it shipped in v0.7.0.)

- [ ] **Step 8: Point the superseded v0.6.0 spec note at this spec**

In `docs/superpowers/specs/2026-05-01-v0.6.0-design.md`, find the "no time prefix" events-table note already marked superseded and update its reference to point at `docs/superpowers/specs/2026-05-31-v0.7.0-design.md` (the spec that reverses it). If it already references "v0.7.0" generically, make it the explicit file path.

- [ ] **Step 9: Validate**

- Run `git grep -n "brain-config.json"` — the check is not "zero matches." Legitimate mentions are expected in `.gitignore` (back-compat), historical specs/plans (`2026-05-01-*`), this v0.7.0 spec/plan, `skills/_shared/path-resolution.md` (the legacy-migration source), and `ROADMAP.md` history. The check fails only if a live skill or current-facing doc presents `.brain-config.json` as the **current source of truth** for config.
- Confirm `plugin.json` reads `0.7.0`.
- Re-read ROADMAP: v0.7.0 under Shipped, Current is v0.8.0 (TBD), no "Graph analytics" in Future.

- [ ] **Step 10: Commit**

```bash
git add .claude-plugin/plugin.json README.md CONTRIBUTING.md CLAUDE.md ROADMAP.md docs/superpowers/specs/2026-05-01-v0.6.0-design.md
git commit -m "docs: align README/CONTRIBUTING/CLAUDE/ROADMAP with v0.7.0; bump version to 0.7.0"
```

---

## Final verification

- [ ] All four skills resolve the path via the new contract (no live `.brain-config.json` dependency).
- [ ] Journey-log bullets carry an `HH:mm` prefix and are gated by the toggle.
- [ ] `skills/_shared/logseq-format.md` exists and is referenced by `journey-log.md` and `brain-save/SKILL.md`.
- [ ] "brain stats" returns aggregate counts; "brain status" still returns the dashboard.
- [ ] `plugin.json` is `0.7.0`; ROADMAP, README, CONTRIBUTING, CLAUDE.md all consistent.
- [ ] Full manual round-trip per `CONTRIBUTING.md` checklist (incl. new items 11–12) passes against a scratch graph.

## Spec coverage check

| Spec section | Task |
|---|---|
| §1 Durable config | Task 1 (+ doc edits Task 5) |
| §2 logseq-format.md | Task 3 |
| §3 HH:mm prefix | Task 2 |
| §4 brain-stats mode | Task 4 |
| Version & docs | Task 5 |
